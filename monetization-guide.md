# 수익화 실행 지침 — Claude Code 운영 매뉴얼

> **목적**: `수익화_전략_가이드`의 전략을 Claude Code가 코드베이스에 반영할 때 따라야 할 규칙과 의사결정 기준을 정의한다. 구체적인 코드 구현과 파일 경로는 Claude Code가 현재 프로젝트의 프레임워크·테마 구조를 분석하여 자체적으로 판단한다.
>
> **애드센스 퍼블리셔 ID**: `ca-pub-9882237042146290`

---

## 1. 애드센스 배치 최적화

### 1-1. 광고 컴포넌트 생성 지침

아래 4종의 광고 컴포넌트를 재사용 가능한 파셜/컴포넌트로 생성한다.

| 컴포넌트 이름 | 용도 | 광고 유형 |
|-------------|------|----------|
| `ad-article-top` | 글 상단 (제목 아래, 목차 위) | 디스플레이 광고 (auto format) |
| `ad-article-bottom` | 글 하단 (본문 끝, 댓글/관련글 위) | 디스플레이 광고 (auto format) |
| `ad-infeed` | 코드 블록 사이 | 인피드/네이티브 광고 (fluid format) |
| `ad-sidebar` | 사이드바 하단 | 디스플레이 광고 (auto format) |

**구현 시 필수 규칙**:

- 모든 광고 코드는 프로덕션 환경에서만 렌더링되도록 환경 조건문으로 감싼다
- `data-ad-client`에 퍼블리셔 ID `ca-pub-9882237042146290` 사용
- `data-ad-slot` 값은 `[SLOT_ID]` 플레이스홀더로 남기고, 사용자에게 애드센스 콘솔에서 발급받은 실제 값 입력을 요청한다
- 반응형 광고 단위(`data-full-width-responsive="true"`)를 기본으로 사용한다

### 1-2. 레이아웃 삽입 위치

포스트(싱글) 레이아웃에서 아래 위치에 광고 컴포넌트를 삽입한다.

| 삽입 위치 | 설명 |
|-----------|------|
| 글 상단 | 본문 콘텐츠 렌더링 직전 |
| 글 하단 | 본문 콘텐츠 렌더링 직후 |
| 사이드바 하단 | 사이드바 컴포넌트 맨 끝 |

### 1-3. 초기 단계 규칙 (일 방문자 500명 미만)

- 글 상단 1개 + 글 하단 1개만 활성화한다
- 사이드바 광고와 인피드 광고는 주석 처리로 준비만 해둔다
- 트래픽이 일 500명을 넘기면 사이드바 → 인피드 순으로 활성화한다

### 1-4. 모바일 대응

- 모바일(600px 이하)에서 사이드바 광고를 숨기는 반응형 CSS를 추가한다

---

## 2. 제휴 마케팅 링크 관리

### 2-1. 제휴 링크 데이터 파일

제휴 링크를 데이터 파일 하나로 중앙 관리한다. 카테고리별로 아래 프로그램을 포함하며, 각 항목에는 `name`, `url`, `description`, `commission`, `priority` 필드를 둔다.

| 카테고리 | 서비스 | 수수료 구조 | 우선순위 |
|----------|--------|------------|---------|
| cloud | DigitalOcean | 가입당 $200+ | 1 |
| cloud | Vultr | 가입당 $35~$100 | 1 |
| devtools | JetBrains | 판매의 25% | 2 |
| devtools | GitHub Copilot | 가입 기반 | 2 |
| hosting | Cloudflare | 사용량 기반 | 3 |
| hosting | Vercel | 가입 기반 | 3 |
| education | Udemy | 판매의 15~45% | 4 |
| education | 인프런 | 판매의 30% | 4 |

**필수 규칙**: `url` 필드에는 `[제휴링크_서비스명]` 플레이스홀더를 넣고, 사용자에게 실제 URL 입력을 요청한다. 절대로 임의의 URL을 생성하지 않는다.

### 2-2. 제휴 링크 컴포넌트

아래 3개 패턴의 재사용 가능한 컴포넌트를 생성한다.

| 컴포넌트 이름 | 패턴 | 사용 맥락 | 데이터 카테고리 |
|-------------|------|----------|---------------|
| `affiliate-cloud-cta` | 실습 환경 안내 | 클라우드/서버 관련 튜토리얼 | cloud |
| `affiliate-tools-recommend` | 추천 개발 도구 | 모든 튜토리얼/실습 글 하단 | devtools |
| `affiliate-learn-more` | 관련 강의 연결 | 입문/학습 주제 결론부 | education |

**컴포넌트 디자인 원칙**:

- 블로그 테마와 어울리는 박스형 UI (어두운 배경, 왼쪽 색상 바)
- 모든 제휴 링크에 `rel="noopener sponsored"` 및 `target="_blank"` 필수
- 제휴 데이터 파일을 반복문으로 렌더링하여 유지보수성 확보

### 2-3. 포스트 작성 시 제휴 링크 삽입 규칙

1. **필수 삽입** (모든 튜토리얼/실습 글): 글 하단에 `affiliate-tools-recommend` 배치
2. **선택 삽입** (주제에 따라):
   - 클라우드/서버 관련 글 → `affiliate-cloud-cta` 를 실습 환경 설명 부분에 배치
   - 학습/입문 주제 글 → `affiliate-learn-more` 를 결론부에 배치
3. **삽입 제한**:
   - 한 글에 제휴 링크 컴포넌트는 최대 2개
   - 주제와 무관한 제휴 링크는 절대 삽입하지 않는다

---

## 3. 포스트 프론트매터 확장

### 3-1. 수익화 메타데이터

기존 프론트매터에 아래 2개 필드를 추가한다 (선택 사항):

- **`affiliate`**: `cloud`, `tools`, `learn` 키로 각 제휴 컴포넌트 활성화 여부를 boolean으로 제어
- **`ads`**: `top`, `bottom`, `infeed` 키로 각 광고 위치 활성화 여부를 boolean으로 제어

### 3-2. 조건부 렌더링

포스트 레이아웃에서 위 프론트매터 값을 읽어 해당 컴포넌트를 조건부로 렌더링한다.

- `ads.top`, `ads.bottom`: 기본값 `true` (명시적으로 `false`를 지정한 경우만 비활성화)
- `affiliate.*`, `ads.infeed`: 기본값 `false` (명시적으로 `true`를 지정한 경우만 활성화)

---

## 4. SEO 수익 연계 체크리스트

### 4-1. 작성 단계 체크리스트

- [ ] SEO 제목 60자 이내, 핵심 키워드 포함
- [ ] 메타 설명(description) 155자 이내
- [ ] 모든 이미지에 alt 텍스트 포함
- [ ] H2/H3 구조화 (키워드 포함)
- [ ] 내부 링크 최소 2개 (관련 포스트)
- [ ] 외부 참조 링크 1개 이상 (공식 문서 등)
- [ ] 목차(TOC) 활성화

### 4-2. 수익화 단계 체크리스트

- [ ] 프론트매터 `affiliate` 및 `ads` 필드 설정
- [ ] 주제에 맞는 제휴 링크 패턴 선택 (최대 2개)
- [ ] 제휴 링크에 `rel="noopener sponsored"` 속성 확인
- [ ] 광고 위치가 콘텐츠 흐름을 방해하지 않는지 확인
- [ ] 제휴 링크 URL이 플레이스홀더가 아닌 실제 URL인지 확인

---

## 5. 슬래시 커맨드 정의

`.claude/commands/` 디렉토리에 아래 커맨드를 생성한다. 각 커맨드의 구체적인 프롬프트 내용은 아래 점검 항목을 기반으로 Claude Code가 작성한다.

### 5-1. `/project:monetize-audit` — 수익화 점검

**점검 항목**:

1. 루트에 ads.txt가 존재하고 올바른 퍼블리셔 ID가 포함되어 있는지
2. 광고 컴포넌트 4종이 모두 생성되어 있는지
3. 포스트 레이아웃에 광고 컴포넌트가 삽입되어 있는지
4. 제휴 데이터 파일에 플레이스홀더가 남아있는지
5. 최근 5개 포스트의 프론트매터에 수익화 필드가 있는지
6. SEO 기본 요소 (description, alt 텍스트, 내부 링크) 여부

**출력**: 각 항목의 상태(✅/❌)와 구체적인 개선 조치를 표로 출력

### 5-2. `/project:add-affiliate` — 기존 글에 제휴 링크 추가

**실행 절차**:

1. `$ARGUMENTS`로 전달된 포스트 파일의 카테고리와 주제를 분석
2. 제휴 데이터 파일에서 관련 제휴 프로그램을 매칭
3. 2-3절의 삽입 규칙에 따라 적합한 컴포넌트를 삽입
4. 프론트매터에 `affiliate` 필드를 추가
5. 한 글에 최대 2개까지만 삽입
6. 변경 내용을 요약하여 출력

### 5-3. `/project:revenue-report` — 수익 현황 리포트

**점검 대상**:

1. **콘텐츠 현황**: 총 포스트 수, 최근 30일 내 신규 포스트 수
2. **수익화 커버리지**: 전체 포스트 중 제휴 링크가 포함된 포스트 비율
3. **로드맵 진척도**:
   - 씨앗 뿌리기 (1~3개월): 콘텐츠 20편+, 제휴 가입 4개+
   - 성장 가속 (4~6개월): 콘텐츠 50편+, 시리즈 3~4개
   - 수익 극대화 (7~12개월): 콘텐츠 100편+, 수익원 3개+
4. **다음 액션**: 현재 단계에서 즉시 실행해야 할 항목 3가지 제안

**출력**: 마크다운 표로 현황을 정리하고, 로드맵 단계에 대한 진척률(%)을 표시

---

## 6. 워크플로우 요약

### 새 포스트 작성 시 (전체 흐름)

```
1. /project:new-post "주제"
   └─ 프론트매터에 affiliate, ads 필드 자동 포함
   └─ 주제 분석 → 적합한 제휴 패턴 자동 삽입

2. 포스트 본문 작성
   └─ SEO 규칙 준수 (제목 60자, 설명 155자, alt 텍스트)
   └─ 내부 링크 최소 2개 삽입

3. /project:seo-audit
   └─ SEO 체크리스트 점검

4. /project:monetize-audit
   └─ 수익화 체크리스트 점검

5. /project:publish
   └─ 빌드 및 배포
```

### 기존 포스트 수익화 보강 시

```
1. /project:revenue-report
   └─ 현재 커버리지 확인, 미적용 포스트 목록 파악

2. /project:add-affiliate "파일경로"
   └─ 포스트별 맞춤 제휴 링크 추가

3. /project:monetize-audit
   └─ 변경 사항 검증

4. /project:publish
   └─ 배포
```

---

## 7. 단계별 활성화 가이드

| 트래픽 기준 | 광고 설정 | 제휴 링크 | 비고 |
|------------|----------|----------|------|
| 일 100명 미만 (현재) | 상단 1 + 하단 1 | 글당 최대 2개 | 콘텐츠 품질 우선 |
| 일 100~500명 | 상단 1 + 하단 1 + 사이드바 | 글당 최대 2개 | 사이드바 활성화 |
| 일 500명 이상 | 전체 활성화 (인피드 포함) | 글당 최대 3개 | 수동 배치 전환 검토 |
| 일 1000명 이상 | 프리미엄 광고 네트워크 검토 | 스폰서드 포스트 | Mediavine/AdThrive 검토 |

---

## 8. 주의 사항

1. **절대 임의 URL 생성 금지**: 제휴 링크, 광고 슬롯 ID는 반드시 사용자에게 실제 값을 요청한다.
2. **광고 과다 배치 금지**: 초기에는 최소한의 광고만 배치하여 페이지 속도와 SEO를 보호한다.
3. **콘텐츠 품질 > 수익화**: 수익화 요소가 콘텐츠의 가독성이나 기술적 깊이를 훼손해서는 안 된다.
4. **프로덕션 환경 체크**: 모든 광고 코드는 프로덕션 환경 조건문으로 감싸서 로컬 개발 시 노출되지 않도록 한다.
5. **`rel="noopener sponsored"`**: 모든 제휴 링크에 반드시 이 속성을 포함한다 (SEO 페널티 방지).
