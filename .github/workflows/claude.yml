name: Claude Code (deprecated)

on:
  issue_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]
  pull_request_review_comment:
    types: [created]

jobs:
  skip:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "deprecated"
